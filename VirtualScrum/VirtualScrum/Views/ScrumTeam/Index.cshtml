@model IEnumerable<VirtualScrum.Models.ScrumTeam>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TeamProjectName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TeamProjectName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ScrumTeamId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ScrumTeamId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ScrumTeamId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
